= Karma Plugin

This is a simple karma plugin. It will parse any (++/--) terms that arrives in channels and computes the
karma.

This plugin uses the Object Storage so you can plug a different Object Storage (like the MongoDB plugin)
to store the karmas outside the JVM.